<!-- app/views/books/index.html.erb -->
<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>作者</th>
      <th>題名</th>
      <th>概要</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Book', new_book_path %>

